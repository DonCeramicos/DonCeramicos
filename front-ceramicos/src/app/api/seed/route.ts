// src/app/api/seed/route.ts
/*
import { NextResponse } from "next/server";
import { seedDatabase } from "@/helpers/seed-bdd";

export async function GET() {
  await seedDatabase();
  return NextResponse.json({ message: "Base de datos poblada con éxito" });
}
*/